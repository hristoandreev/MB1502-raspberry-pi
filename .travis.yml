sudo: required
dist: trusty
language: c
compiler:
  - gcc
cache:
  directories:
    - "${HOME}/persist"

before_script:
# Extra gcc versions
  # For teensy build
  - sudo apt-get install gcc-arm-linux-gnueabihf
  - sudo apt-get install git-core
  - git clone git://git.drogon.net/wiringPi
  - cd wiringPi
  - git pull origin
  - ./build
  - pwd
script:
  - cd ..
  - arm-linux-gnueabihf-gcc -I${HOME}/wiringPi/wiringPi main.c -o spi_synth -lwiringPi
after_failure:
